Place Microtec reconstruction TIFFs here for local conversion. Image files and
generated NRRD/XML outputs are ignored by Git.

The supplied `example.tiff` is retained locally (850 MiB), not committed.

```sh
./build/release/mito_unpack --output-dir converted examples/example.tiff
```
